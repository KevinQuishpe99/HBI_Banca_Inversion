/** Clases Tailwind compartidas para formularios (mantener consistencia y DRY). */
export const formInput =
  'w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 text-gray-900';

export const formInputBlue = `${formInput} focus:ring-blue-500 focus:border-blue-500`;

export const formInputPurple = `${formInput} focus:ring-purple-500`;

export const formLabel = 'block text-sm font-medium text-gray-700 mb-1';
